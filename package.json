{
  "name": "damei_admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "publish":"export NODE_ENV=production && gulp js_lib && gulp css_lib && gulp font_lib",
    "build":"gulp js_lib && gulp css_lib && gulp font_lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sh bin/jetty.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0",
    "gulp-cssnano": "^2.1.2",
    "gulp-uglify": "^2.0.0"
  }
}
